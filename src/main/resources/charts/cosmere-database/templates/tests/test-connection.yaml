apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosmere-database.fullname" . }}-test-connection"
  labels:
    {{- include "cosmere-database.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosmere-database.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
